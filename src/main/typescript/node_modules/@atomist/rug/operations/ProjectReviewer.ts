import {RugOperation, Result, Parameter} from "./RugOperation"
import {Services} from "../model/Core"

/**
 * Review the given project with given parameters
 */
export interface ProjectReviewer extends RugOperation {

  review(project: Project, params: Object): Result
}
